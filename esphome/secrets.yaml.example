# ESPHome Secrets for Naboo CSI Sensors
# Copy to your ESPHome secrets.yaml or set these values

wifi_password: "CHANGEME"
api_key: ""      # Generate with: python3 -c "import secrets,base64; print(base64.b64encode(secrets.token_bytes(32)).decode())"
ota_password: ""  # Set a password for OTA updates
